/** RESTORED ORIGINAL FROM GIT - no price changes to avoid further issues */
import { NextPage } from 'next';

const PropertyDetail: NextPage = () => {
	return null;
};

export default PropertyDetail;
